# Very simple everyday git commands

```
# show status
git status

# add files
git add <filename>

# commit files with message
git commit -m "<message here>"

# show changes in a file
git diff <filename>

# push files to github
git push origin <branch name>

#test change

```